---
description: Python 基础约定（语法、导入、包结构）
alwaysApply: true
---

# Python 约定

目标版本：**Python 3.14+**（`pyproject.toml`）。新代码使用现代语法，避免 `typing.Optional` / `List` / `Dict` 等旧写法（除非与第三方 API 兼容必须保留）。

## 现代类型语法

```python
# 推荐
def find_user(user_id: str | None = None) -> dict[str, Any]:
    ...

# 不推荐
from typing import Optional, Dict, Any
def find_user(user_id: Optional[str] = None) -> Dict[str, Any]:
    ...
```

- 领域实体：`@dataclass(frozen=True, slots=True)`，不含 ORM
- 仓储接口：`typing.Protocol`，实现在 `infrastructure/storage/` 或对应 adapter
- HTTP DTO：Pydantic `BaseModel`，放 `interfaces/http/schemas/`

## import

优先**显式模块路径**，避免隐式包聚合导入：

```python
# 推荐
from config.database import DatabaseConfig
from config.config import config

# 不推荐
from app.database.DatabaseConfig import async_connections
```

- 禁止 `python config/xxx.py`，包内入口用 `python -m`（如 `python -m app.main`）
- 路径常量放项目根 `paths.py`（`BASE_DIR`、`ENV_FILE`、`STORAGE_DIR`）

## `__init__.py`

1. 新建包目录一定要有 `__init__.py`；默认为空。
2. `__init__.py` 文件中严格不可以导出，不做 re-export。

```python
# 正确：空文件或仅包级 docstring

# 错误
from .db import get_db
__all__ = ["get_db"]
```

## 日志与数据库

```python
import logging
logger = logging.getLogger(__name__)  # 业务 → app.log
```

- HTTP 访问：`app.request`；领域异常：`app.channel.exception`
- 结构化字段用 `extra={...}`，不写 f-string 拼进 message
- 应用层通过 `SessionProvider` 获取 session，写操作成功后 `await session.commit()`
- 异常时 `SessionProvider` 自动 rollback，Service 内不要吞掉异常

## 工具链

- 行宽 **150**（`pyproject.toml` 中 `tool.black` / `tool.ruff`）
- Ruff 已启用 `UP` 规则，优先用 `match`/`case` 等现代语法
- 提交前：`uv run black .` 与 `uv run ruff check .`
