---
description: HTTP 接口层约定（Agent API）
globs: app/interfaces/http/**/*
alwaysApply: false
---

# HTTP 接口层

## 路由注册链

```
main.py → routers/register.py → api/router (/api) → v1/router (/v1) → endpoints/
                              → ws/router (/ws) → v1/router → endpoints/
```

| 前缀 | 模块 |
|------|------|
| `/api/v1/chat` | 对话 REST |
| `/api/v1/documents` | 文档管理 |
| `/api/v1/knowledge-bases` | 知识库 |
| `/api/v1/skills` | 技能 |
| `/ws/v1/chat` | 流式对话 WebSocket |

## 两种响应形态

### REST 管理接口

使用 `JsonResponse` + Presenter，路由保持薄：

```python
service: ChatService = Depends(get_chat_service)
return JsonResponse.success(data=to_response(conversation))
```

### 流式对话

WebSocket 地址：`/ws/v1/chat`

客户端发送一条 JSON（同 REST `ChatRequest`），服务端推送：

```json
{"event": "start", "data": {"provider": "...", "model": "...", "trace_id": "..."}}
{"event": "delta", "data": {"content": "..."}}
{"event": "done", "data": {"reply": "...", "model": {...}}}
{"event": "error", "data": {"message": "...", "trace_id": "..."}}
```

endpoint 内不直接调用 LangGraph / LangChain，只调 `ChatService.stream_message`。

## Schemas / Presenter / Deps

| 目录 | 职责 |
|------|------|
| `schemas/requests/` | 入参 Pydantic 模型 |
| `schemas/responses/` | 出参 Pydantic 模型 |
| `presenters/` | Entity → Response DTO |
| `deps/` | 注入 application 服务 |

## 领域错误映射

- 领域异常继承 `app.domain.exceptions.DomainError`
- 在 `handlers/domain_error.py` 的 `_resolve_error_code` 追加映射
- 子模块异常放在 `domain/{module}/exceptions.py`

## 中间件注意

- `RequestScope` 须在 `RequestLog` **外侧**（后注册）
- `CorsMiddleware` 须在最外层

## 禁止

- endpoint 内直接调用 LangGraph / LangChain
- endpoint 内写工具实现或向量检索逻辑
