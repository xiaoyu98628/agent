---
description: agent 项目约定与当前进度
alwaysApply: true
---

# 项目约定（DDD）

## 目录结构

```
agent/
├── paths.py                         # BASE_DIR、ENV_FILE、DATABASE_DIR
├── config/                          # 配置（跨层共享）
│   ├── settings.py                  # BASE_SETTINGS_CONFIG
│   ├── app.py / database.py / logging.py / cors.py
│   └── config.py                    # config() 聚合入口
├── app/
│   ├── main.py                      # 应用入口 / lifespan
│   ├── domain/                      # 领域层：实体、枚举、异常、仓储接口
│   │   └── user/
│   ├── application/                 # 应用层：用例、应用服务
│   │   ├── user/
│   │   └── support/                 # 分页、ULID 等
│   ├── interfaces/                  # 接口层：HTTP 等
│   │   └── http/
│   │       ├── api/v1/endpoints/    # REST
│   │       ├── ws/v1/               # WebSocket（待扩展）
│   │       ├── routers/             # register.py 挂载入口
│   │       ├── middleware/
│   │       ├── handlers/            # 异常 handler（domain_error 等）
│   │       ├── deps/                # FastAPI Depends 注入
│   │       ├── presenters/          # Entity → Response DTO
│   │       ├── schemas/             # requests / responses
│   │       └── support/
│   │           ├── codec/
│   │           └── response/        # JsonResponse、码表
│   └── infrastructure/
│       ├── context/                 # request_scope / trace_id
│       ├── logging/                 # LogManager、formatters
│       ├── database/                # 连接、SessionProvider
│       │   ├── orm/base.py
│       │   └── connectors/
│       └── persistence/
│           ├── models/
│           ├── repositories/        # 仓储 SQLAlchemy 实现
│           └── registry.py
└── database/                        # Alembic 迁移
```

## 分层依赖（单向）

```
interfaces → application → domain
                ↓              ↑
         infrastructure ──────┘
              （实现 domain 仓储接口；ORM 不得反向依赖 application/interfaces）
```

- **domain/**：纯业务概念，不依赖 SQLAlchemy / FastAPI。
- **application/**：编排用例，依赖 domain 接口；通过 `SessionProvider` 管理事务。
- **infrastructure/persistence/**：ORM Model、仓储实现、`registry.py`。
- **infrastructure/database/**：连接管理，Laravel Database 设计思想。
- **interfaces/**：路由、中间件、DTO、Presenter、统一响应；不得包含业务编排逻辑。
- **interfaces/http/support/response/**：对外 API 契约，不属于 infrastructure。
- **config/logging.py**：`LOG_` env + `LoggingConfig.channels`；装配见 `infrastructure/logging/manager`。
- **config/cors.py**：`CORS_` env；中间件 `CorsMiddleware` 须在最外层。

## 配置模式

```python
from config.config import config
configure = config()
```

- `BASE_SETTINGS_CONFIG` 放在 `config/settings.py`。
- `config/config.py` 只做聚合（app / database / logging / cors）。
- env 前缀：`APP_`、`DB_`、`LOG_`、`CORS_`；模板见 `.env.sample`。
- `APP_SERVICE_CODE`（`config/app.py` → `service_code`）：三位数字服务码，参与响应码组装，默认 `001`。

## 新增模块（以 User 为模板）

1. `domain/{m}/` — entity, enums, exceptions, repository (Protocol)
2. `infrastructure/persistence/models/{m}.py` + `repositories/{m}.py`
3. `persistence/registry.py` 导入 Model
4. `application/{m}/service.py`
5. `interfaces/http/` — schemas, presenters, deps, `api/v1/endpoints/{m}.py`
6. `api/v1/router.py` 挂载；`handlers/domain_error.py` 映射异常

## 统一 API 响应

### 日志

- 启动：`configure_logging()` → `LogManager` 读 `LoggingConfig.channels` 装配 handler。
- **路径**：各通道 `path` 在 `config/logging.py` → `LoggingConfig.channels` 定义（默认 `storage/logs/{APP_NAME}/`）。
- **文件**：`app.log`、`request.log`、`db.log`、`exception.log`；`LOG_LEVEL` 控制所有通道。
- **轮转**：`LOG_DRIVER=single|daily|rotating`
- **访问日志**：`request.log` 每个请求两条（request / response）。
- **请求上下文**：`infrastructure/context/request_scope.py`；`RequestScopeMiddleware` 绑定/重置。
- **trace_id**：`TraceIdFilter` 读 `get_trace_id()`，写入日志行与 `JsonResponse`。
- **request**：`logging.getLogger("app.request")`；**exception**：`app.channel.exception`。
- **domain**：默认不打日志。

### 响应体结构（`interfaces/http/support/response/json.py`）

```python
from app.interfaces.http.support.response.json import JsonResponse
from app.interfaces.http.support.response.code.success_code import SuccessCode
from app.interfaces.http.support.response.code.error_code import ErrorCode

return JsonResponse.success(data={"id": 1})
return JsonResponse.success(data=user, code=SuccessCode.SUCCESS_CREATED)
return JsonResponse.error(code=ErrorCode.NOT_FOUND_ERROR)
```

字段：`code`（10 位字符串）、`success`（alias，`is_success`）、`message`、`data`、`trace_id`。

路由直接 `return JsonResponse.success(...)`；保持路由薄，业务逻辑在 application，Entity 转 DTO 走 presenter。

### 10 位响应码格式

```
[ HTTP 3位 ][ 服务码 3位 ][ 低位 4位 ]
 200          001           0000     → "2000010000"
 422          001           0101     → "4220010101"
```

- **低位 4 位**：在 `CodeDefinition.code` 中定义。
- **完整 10 位**：由 `CodedEnum.full_code()` 组装，读取 `config().app.service_code`；**不需要单独的 builder 模块**。
- `code` 字段用 **字符串** 返回，避免前导 `0` 丢失。

### 码表约定（`interfaces/http/support/response/code/`）

| 文件 | 类 | 用途 |
|------|-----|------|
| `contract.py` | `CodeDefinition` | frozen dataclass：`code`（4 位低位）、`message`、`status_code` |
| `contract.py` | `CodedEnum` | Enum 基类；提供 `code` / `message` / `status_code` / `full_code()` |
| `success_code.py` | `SuccessCode` | 通用成功低位 |
| `error_code.py` | `ErrorCode` | 通用 API 错误低位 |

新增码表：继承 `CodedEnum`，成员一行一个 `CodeDefinition`；**不要在每个 Enum 重复写 property**。

低位分段建议：`0xxx` 成功/通用；`1xxx` user；`2xxx` order。

### 业务错误码放哪

| 层级 | 放什么 | 格式 |
|------|--------|------|
| `domain/{ctx}/` | 业务语义异常（`DomainError` 子类） | 不含 HTTP、不含服务码 |
| `interfaces/http/support/response/code/` | 对外 API 码表 | `CodeDefinition` + `CodedEnum` |
| `handlers/domain_error.py` | 领域异常 → `ErrorCode` | 映射后 `JsonResponse.error(...)` |

领域层**不**继承 `CodedEnum`。

## HTTP 路由分包

```
interfaces/http/
├── routers/register.py      # app.include_router 入口
├── api/v1/router.py         # /api/v1
├── api/v1/endpoints/        # REST（如 user.py）
└── ws/v1/                   # WebSocket（待扩展）
```

- REST：`/api/v1/...` — endpoints 建模块，`api/v1/router.py` 挂载。
- WebSocket：`/ws/v1/...` — 待扩展。
- 中间件顺序（内→外）：`RequestLog` → `RequestScope` → `QueryParamDecode` → `Cors`（最外）。

## 数据库连接

```python
from app.infrastructure.database.db import DB

async with DB.connection() as session:
    ...
```

应用层推荐 `SessionProvider`（见 `application/user/service.py`）。

迁移：`app/infrastructure/database/sync.py` → `database/migrations/env.py`

```bash
alembic -c database/alembic.ini revision --autogenerate -m "..."
alembic -c database/alembic.ini upgrade head
```

## ORM Model（持久化模型）

- 放在 `infrastructure/persistence/models/`，继承 `infrastructure/database/orm/base.py` 的 `Base`。
- 领域枚举从 `domain/` 引用，例如 `from app.domain.user.enums import UserStatus`。
- 新建 Model 后在 `persistence/registry.py` 导入。

## 当前进度

- [x] 数据库连接层
- [x] Alembic + sync_url
- [x] DDD 目录骨架
- [x] 持久化 Base + User ORM + 仓储实现
- [x] 统一响应（`JsonResponse` + `CodedEnum` + `SuccessCode` / `ErrorCode`）
- [x] HTTP 路由分包（`api/v1`、`ws/v1`）
- [x] domain 实体 / 仓储接口（User）
- [x] application 用例（UserService）
- [x] 领域异常 handler（`DomainError` → `JsonResponse.error`）
- [x] 日志（`LogManager` + channels + `request.log`）
- [x] 中间件（`request_scope`、trace_id、`request_log`、CORS）
- [ ] 全局异常 handler 补全（422 / 500 / `HTTPException` 等）
- [ ] 测试与 CI
