---
description: agent 项目约定（Agent-only 架构）
alwaysApply: true
---

# 项目约定（Agent-only）

## 项目定位

个人 Agent + 线上 Agent 服务的统一运行时。基于 FastAPI 暴露 HTTP/WS API，LangGraph 负责 Agent 编排。

- **personal 模式**：本地 CLI / 本机 API，全工具、本地 storage
- **server 模式**：线上部署，API Key 鉴权、沙箱工具、多 workspace 隔离

## 目录结构

```
agent/
├── paths.py                         # BASE_DIR、ENV_FILE、STORAGE_DIR
├── config/
│   ├── settings.py                  # BASE_SETTINGS_CONFIG
│   ├── app.py                       # + deployment_mode
│   ├── llm.py                       # 模型、API Key
│   ├── agent.py                     # 工具开关、迭代上限
│   ├── rag.py                       # chunk、top_k、向量库
│   ├── logging.py / cors.py
│   └── config.py                    # config() 聚合入口
├── app/
│   ├── main.py                      # FastAPI 入口
│   ├── domain/
│   │   ├── exceptions.py            # DomainError 基类
│   │   ├── workspace/               # 租户边界
│   │   ├── conversation/            # 会话、消息
│   │   ├── document/                # 文档、分块
│   │   ├── knowledge/               # 知识库
│   │   ├── skill/                   # 技能
│   │   └── memory/                  # 长期记忆
│   ├── application/
│   │   ├── chat/                    # 对话用例门面
│   │   ├── agent/                   # LangGraph 编排
│   │   └── rag/                     # 入库、检索
│   ├── infrastructure/
│   │   ├── llm/                     # LangChain ChatModel
│   │   ├── vector/                  # pgvector / Qdrant
│   │   ├── document/                # PDF/Markdown loader
│   │   ├── tools/                   # web、file、terminal…
│   │   ├── storage/                 # 本地 / S3
│   │   ├── sandbox/                 # 代码执行隔离
│   │   ├── logging/                 # LogManager
│   │   └── context/                 # trace_id、request_scope
│   └── interfaces/
│       ├── http/
│       │   ├── api/v1/endpoints/    # chat、documents、knowledge_bases、skills
│       │   ├── ws/v1/endpoints/     # chat_stream
│       │   ├── routers/、middleware/、handlers/
│       │   ├── deps/、presenters/、schemas/
│       │   └── support/response/    # JsonResponse、码表
│       ├── cli/                     # 个人模式客户端
│       └── gateway/                 # Telegram 等（后期）
└── storage/                         # 本地文件、日志（运行时生成）
```

## 分层依赖（单向）

```
interfaces → application → domain
                ↑
         infrastructure
```

- **domain/**：纯 Agent 业务概念，不依赖 LangChain / FastAPI / SQLAlchemy。
- **application/**：编排用例；`chat` 调 `agent` / `rag`，不直接处理 HTTP。
- **infrastructure/**：LLM、工具、向量、存储、沙箱等适配器实现。
- **interfaces/**：HTTP/WS/CLI/Gateway 入口，保持薄；REST 管理接口走 JsonResponse，流式对话走 WS/SSE 事件。

## 调用链

```
CLI / HTTP / WS / Gateway
        ↓
application/chat/
        ↓
application/agent/（LangGraph）
        ↓
application/rag/（按需）
        ↓
infrastructure/tools | llm | vector | sandbox | storage
```

## 配置模式

```python
from config.config import config
configure = config()
```

- env 前缀：`APP_`、`LLM_`、`AGENT_`、`RAG_`、`LOG_`、`CORS_`；模板见 `.env.sample`。
- `APP_DEPLOYMENT_MODE`：`personal` | `server`
- `config/config.py` 聚合 app / llm / agent / rag / logging / cors

## 新增模块

以 **conversation** 或 **workspace** 为模板：

1. `domain/{m}/` — entity, enums, exceptions, repository（Protocol，按需）
2. `application/{m}/` 或扩展现有 chat/agent/rag
3. `infrastructure/` — 具体实现（storage、vector 等）
4. `interfaces/http/` — schemas, presenters, deps, endpoints
5. `handlers/domain_error.py` — 领域异常映射（按需）

## 部署模式差异

| 配置 | personal | server |
|------|----------|--------|
| 鉴权 | 可省略 | API Key → workspace |
| 工具 | `AGENT_DEFAULT_TOOL_POLICY=full` | `sandbox` |
| terminal | 允许 | 禁止 |
| 存储 | 本地 `storage/` | S3 / 对象存储 |

## 日志

- 启动：`configure_logging()` → `LogManager`
- 路径：`storage/logs/{APP_NAME}/`
- 通道：`app.log`、`request.log`、`exception.log`
- 中间件顺序（内→外）：`RequestLog` → `RequestScope` → `QueryParamDecode` → `Cors`

## 当前进度

- [x] Agent-only 目录骨架
- [x] 配置（app / llm / agent / rag）
- [x] 用户自选模型（ModelSelection + llm/factory + static catalog）
- [x] POST /api/v1/chat、GET /api/v1/chat/models/options
- [x] WebSocket 流式 /ws/v1/chat
- [x] Conversation 持久化（SQLite）
- [x] 工具注册（file / web / terminal）
- [x] CLI 个人模式
- [x] RAG + SQLite 向量检索
- [x] Skills + Memory
- [x] Gateway（Telegram）
